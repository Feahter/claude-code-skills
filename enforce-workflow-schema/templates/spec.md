# <TASK-ID>: <一句话需求标题>

## 需求摘要
<3-5 行写清楚：谁、为什么、要干什么。复杂度直接来自代码上下文的，给出关键文件路径与 line 引用>

## 输入输出
- **入口**：<用户从哪里触发，对应路由 / 组件 / 接口 / 命令>
- **输入**：<参数 / 字段 / 用户操作>
- **输出**：<UI 表现 / 接口返回 / 副作用>
- **关键边界**：<空值 / 异常 / 并发 / 超时 行为>

## 排除范围（不做什么）
> 这一段是硬要求。不写清楚，AI 会"好心"多做。
- <例：不修改 X 模块的现有 API 签名>
- <例：不引入新的第三方依赖>
- <例：不重构 Y 函数（即使它看起来糟）>
- <例：不补现有 any 类型的收紧>
- <例：不动 i18n key 的命名约定>

## 验收口径
<可被一个测试用例或一段命令证伪的标准>
- [ ] 验收点 1（例：调 `yarn test src/foo.test.ts` 全绿）
- [ ] 验收点 2（例：手动操作 X 流程，UI 表现 Y）
- [ ] 验收点 3（例：现有测试套件 `yarn test` 仍全绿）

## 依赖与风险
- **上游依赖**：<等待的接口 / 设计稿 / 第三方>
- **风险等级**：<LOW / MEDIUM / HIGH / CRITICAL，参考 risk-and-verify.md>
- **风险点**：<可能的回归路径 / 性能影响 / 数据兼容性>
